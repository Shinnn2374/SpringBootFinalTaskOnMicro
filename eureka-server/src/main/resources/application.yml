server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: eureka-server
    prefer-ip-address: true

spring:
  application:
    name: eureka-server